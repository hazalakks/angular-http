import { HeaderTransformPipe } from './header-transform.pipe';

describe('HeaderTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
